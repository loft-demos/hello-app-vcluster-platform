# Default values for helloWorld.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: ghcr.io/loft-demos/hello-app-vcluster-platform
  pullPolicy: IfNotPresent
  # use Chart appVersion if not set as value
  tag: ""
  args:
    text: echo v1
imagePullSecrets:
  - name: ghcr-login-secret
nameOverride: ""
fullnameOverride: ""
deployment:
  # used to specify commit sha in CI job
  commit: b92cf92aa02d11f37838a4d3bc6807443b4b7a60
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: echo-sa
podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
# capabilities:
#   drop:
#   - ALL
# readOnlyRootFilesystem: true
# runAsNonRoot: true
# runAsUser: 1000

autoscaling:
  enabled: false
service:
  type: ClusterIP
  port: 8080
ingress:
  enabled: true
  hosts:
    - host: hello-app-vcluster-platform-vcluster-platform.us.demo.dev
      paths:
        - backend:
            service:
              name: hello-app-vcluster-platform
              port:
                name: HTTP
          path: /
          pathType: Prefix
  tls:
    - hosts:
        - hello-app-vcluster-platform-vcluster-platform.us.demo.dev
resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
nodeSelector: {}
tolerations: []
affinity: {}
